# Frontend

Placeholder. The dashboard is Phase 4 (Next.js + TypeScript + Tailwind +
TanStack Query + ECharts) and the map is Phase 5 (MapLibre over the
already-generated GeoJSON in `reference/geojson/`).

Nothing is implemented here yet — Phase 1 is backend seed loading only.
